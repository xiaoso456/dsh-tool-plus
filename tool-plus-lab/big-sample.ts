// Big synthetic TS file to trigger structural summary
export interface Big { id: number; payload: string }

export function fn1(a: number, b: string): string {
  const mid = a * 1 + b.length;
  if (mid > 000) { console.log('branch 0', mid); }
  if (mid > 100) { console.log('branch 1', mid); }
  if (mid > 200) { console.log('branch 2', mid); }
  if (mid > 300) { console.log('branch 3', mid); }
  if (mid > 400) { console.log('branch 4', mid); }
  if (mid > 500) { console.log('branch 5', mid); }
  return `r${i}-${mid}`;
}

export function fn2(a: number, b: string): string {
  const mid = a * 2 + b.length;
  if (mid > 000) { console.log('branch 0', mid); }
  if (mid > 100) { console.log('branch 1', mid); }
  if (mid > 200) { console.log('branch 2', mid); }
  if (mid > 300) { console.log('branch 3', mid); }
  if (mid > 400) { console.log('branch 4', mid); }
  if (mid > 500) { console.log('branch 5', mid); }
  return `r${i}-${mid}`;
}

export function fn3(a: number, b: string): string {
  const mid = a * 3 + b.length;
  if (mid > 000) { console.log('branch 0', mid); }
  if (mid > 100) { console.log('branch 1', mid); }
  if (mid > 200) { console.log('branch 2', mid); }
  if (mid > 300) { console.log('branch 3', mid); }
  if (mid > 400) { console.log('branch 4', mid); }
  if (mid > 500) { console.log('branch 5', mid); }
  return `r${i}-${mid}`;
}

export function fn4(a: number, b: string): string {
  const mid = a * 4 + b.length;
  if (mid > 000) { console.log('branch 0', mid); }
  if (mid > 100) { console.log('branch 1', mid); }
  if (mid > 200) { console.log('branch 2', mid); }
  if (mid > 300) { console.log('branch 3', mid); }
  if (mid > 400) { console.log('branch 4', mid); }
  if (mid > 500) { console.log('branch 5', mid); }
  return `r${i}-${mid}`;
}

export function fn5(a: number, b: string): string {
  const mid = a * 5 + b.length;
  if (mid > 000) { console.log('branch 0', mid); }
  if (mid > 100) { console.log('branch 1', mid); }
  if (mid > 200) { console.log('branch 2', mid); }
  if (mid > 300) { console.log('branch 3', mid); }
  if (mid > 400) { console.log('branch 4', mid); }
  if (mid > 500) { console.log('branch 5', mid); }
  return `r${i}-${mid}`;
}

export function fn6(a: number, b: string): string {
  const mid = a * 6 + b.length;
  if (mid > 000) { console.log('branch 0', mid); }
  if (mid > 100) { console.log('branch 1', mid); }
  if (mid > 200) { console.log('branch 2', mid); }
  if (mid > 300) { console.log('branch 3', mid); }
  if (mid > 400) { console.log('branch 4', mid); }
  if (mid > 500) { console.log('branch 5', mid); }
  return `r${i}-${mid}`;
}

export function fn7(a: number, b: string): string {
  const mid = a * 7 + b.length;
  if (mid > 000) { console.log('branch 0', mid); }
  if (mid > 100) { console.log('branch 1', mid); }
  if (mid > 200) { console.log('branch 2', mid); }
  if (mid > 300) { console.log('branch 3', mid); }
  if (mid > 400) { console.log('branch 4', mid); }
  if (mid > 500) { console.log('branch 5', mid); }
  return `r${i}-${mid}`;
}

export function fn8(a: number, b: string): string {
  const mid = a * 8 + b.length;
  if (mid > 000) { console.log('branch 0', mid); }
  if (mid > 100) { console.log('branch 1', mid); }
  if (mid > 200) { console.log('branch 2', mid); }
  if (mid > 300) { console.log('branch 3', mid); }
  if (mid > 400) { console.log('branch 4', mid); }
  if (mid > 500) { console.log('branch 5', mid); }
  return `r${i}-${mid}`;
}

export function fn9(a: number, b: string): string {
  const mid = a * 9 + b.length;
  if (mid > 000) { console.log('branch 0', mid); }
  if (mid > 100) { console.log('branch 1', mid); }
  if (mid > 200) { console.log('branch 2', mid); }
  if (mid > 300) { console.log('branch 3', mid); }
  if (mid > 400) { console.log('branch 4', mid); }
  if (mid > 500) { console.log('branch 5', mid); }
  return `r${i}-${mid}`;
}

export function fn10(a: number, b: string): string {
  const mid = a * 10 + b.length;
  if (mid > 000) { console.log('branch 0', mid); }
  if (mid > 100) { console.log('branch 1', mid); }
  if (mid > 200) { console.log('branch 2', mid); }
  if (mid > 300) { console.log('branch 3', mid); }
  if (mid > 400) { console.log('branch 4', mid); }
  if (mid > 500) { console.log('branch 5', mid); }
  return `r${i}-${mid}`;
}

export function fn11(a: number, b: string): string {
  const mid = a * 11 + b.length;
  if (mid > 000) { console.log('branch 0', mid); }
  if (mid > 100) { console.log('branch 1', mid); }
  if (mid > 200) { console.log('branch 2', mid); }
  if (mid > 300) { console.log('branch 3', mid); }
  if (mid > 400) { console.log('branch 4', mid); }
  if (mid > 500) { console.log('branch 5', mid); }
  return `r${i}-${mid}`;
}

export function fn12(a: number, b: string): string {
  const mid = a * 12 + b.length;
  if (mid > 000) { console.log('branch 0', mid); }
  if (mid > 100) { console.log('branch 1', mid); }
  if (mid > 200) { console.log('branch 2', mid); }
  if (mid > 300) { console.log('branch 3', mid); }
  if (mid > 400) { console.log('branch 4', mid); }
  if (mid > 500) { console.log('branch 5', mid); }
  return `r${i}-${mid}`;
}

export function fn13(a: number, b: string): string {
  const mid = a * 13 + b.length;
  if (mid > 000) { console.log('branch 0', mid); }
  if (mid > 100) { console.log('branch 1', mid); }
  if (mid > 200) { console.log('branch 2', mid); }
  if (mid > 300) { console.log('branch 3', mid); }
  if (mid > 400) { console.log('branch 4', mid); }
  if (mid > 500) { console.log('branch 5', mid); }
  return `r${i}-${mid}`;
}

export function fn14(a: number, b: string): string {
  const mid = a * 14 + b.length;
  if (mid > 000) { console.log('branch 0', mid); }
  if (mid > 100) { console.log('branch 1', mid); }
  if (mid > 200) { console.log('branch 2', mid); }
  if (mid > 300) { console.log('branch 3', mid); }
  if (mid > 400) { console.log('branch 4', mid); }
  if (mid > 500) { console.log('branch 5', mid); }
  return `r${i}-${mid}`;
}

export function fn15(a: number, b: string): string {
  const mid = a * 15 + b.length;
  if (mid > 000) { console.log('branch 0', mid); }
  if (mid > 100) { console.log('branch 1', mid); }
  if (mid > 200) { console.log('branch 2', mid); }
  if (mid > 300) { console.log('branch 3', mid); }
  if (mid > 400) { console.log('branch 4', mid); }
  if (mid > 500) { console.log('branch 5', mid); }
  return `r${i}-${mid}`;
}

export function fn16(a: number, b: string): string {
  const mid = a * 16 + b.length;
  if (mid > 000) { console.log('branch 0', mid); }
  if (mid > 100) { console.log('branch 1', mid); }
  if (mid > 200) { console.log('branch 2', mid); }
  if (mid > 300) { console.log('branch 3', mid); }
  if (mid > 400) { console.log('branch 4', mid); }
  if (mid > 500) { console.log('branch 5', mid); }
  return `r${i}-${mid}`;
}

export function fn17(a: number, b: string): string {
  const mid = a * 17 + b.length;
  if (mid > 000) { console.log('branch 0', mid); }
  if (mid > 100) { console.log('branch 1', mid); }
  if (mid > 200) { console.log('branch 2', mid); }
  if (mid > 300) { console.log('branch 3', mid); }
  if (mid > 400) { console.log('branch 4', mid); }
  if (mid > 500) { console.log('branch 5', mid); }
  return `r${i}-${mid}`;
}

export function fn18(a: number, b: string): string {
  const mid = a * 18 + b.length;
  if (mid > 000) { console.log('branch 0', mid); }
  if (mid > 100) { console.log('branch 1', mid); }
  if (mid > 200) { console.log('branch 2', mid); }
  if (mid > 300) { console.log('branch 3', mid); }
  if (mid > 400) { console.log('branch 4', mid); }
  if (mid > 500) { console.log('branch 5', mid); }
  return `r${i}-${mid}`;
}

export function fn19(a: number, b: string): string {
  const mid = a * 19 + b.length;
  if (mid > 000) { console.log('branch 0', mid); }
  if (mid > 100) { console.log('branch 1', mid); }
  if (mid > 200) { console.log('branch 2', mid); }
  if (mid > 300) { console.log('branch 3', mid); }
  if (mid > 400) { console.log('branch 4', mid); }
  if (mid > 500) { console.log('branch 5', mid); }
  return `r${i}-${mid}`;
}

export function fn20(a: number, b: string): string {
  const mid = a * 20 + b.length;
  if (mid > 000) { console.log('branch 0', mid); }
  if (mid > 100) { console.log('branch 1', mid); }
  if (mid > 200) { console.log('branch 2', mid); }
  if (mid > 300) { console.log('branch 3', mid); }
  if (mid > 400) { console.log('branch 4', mid); }
  if (mid > 500) { console.log('branch 5', mid); }
  return `r${i}-${mid}`;
}

export function fn21(a: number, b: string): string {
  const mid = a * 21 + b.length;
  if (mid > 000) { console.log('branch 0', mid); }
  if (mid > 100) { console.log('branch 1', mid); }
  if (mid > 200) { console.log('branch 2', mid); }
  if (mid > 300) { console.log('branch 3', mid); }
  if (mid > 400) { console.log('branch 4', mid); }
  if (mid > 500) { console.log('branch 5', mid); }
  return `r${i}-${mid}`;
}

export function fn22(a: number, b: string): string {
  const mid = a * 22 + b.length;
  if (mid > 000) { console.log('branch 0', mid); }
  if (mid > 100) { console.log('branch 1', mid); }
  if (mid > 200) { console.log('branch 2', mid); }
  if (mid > 300) { console.log('branch 3', mid); }
  if (mid > 400) { console.log('branch 4', mid); }
  if (mid > 500) { console.log('branch 5', mid); }
  return `r${i}-${mid}`;
}

export function fn23(a: number, b: string): string {
  const mid = a * 23 + b.length;
  if (mid > 000) { console.log('branch 0', mid); }
  if (mid > 100) { console.log('branch 1', mid); }
  if (mid > 200) { console.log('branch 2', mid); }
  if (mid > 300) { console.log('branch 3', mid); }
  if (mid > 400) { console.log('branch 4', mid); }
  if (mid > 500) { console.log('branch 5', mid); }
  return `r${i}-${mid}`;
}

export function fn24(a: number, b: string): string {
  const mid = a * 24 + b.length;
  if (mid > 000) { console.log('branch 0', mid); }
  if (mid > 100) { console.log('branch 1', mid); }
  if (mid > 200) { console.log('branch 2', mid); }
  if (mid > 300) { console.log('branch 3', mid); }
  if (mid > 400) { console.log('branch 4', mid); }
  if (mid > 500) { console.log('branch 5', mid); }
  return `r${i}-${mid}`;
}

export function fn25(a: number, b: string): string {
  const mid = a * 25 + b.length;
  if (mid > 000) { console.log('branch 0', mid); }
  if (mid > 100) { console.log('branch 1', mid); }
  if (mid > 200) { console.log('branch 2', mid); }
  if (mid > 300) { console.log('branch 3', mid); }
  if (mid > 400) { console.log('branch 4', mid); }
  if (mid > 500) { console.log('branch 5', mid); }
  return `r${i}-${mid}`;
}

export function fn26(a: number, b: string): string {
  const mid = a * 26 + b.length;
  if (mid > 000) { console.log('branch 0', mid); }
  if (mid > 100) { console.log('branch 1', mid); }
  if (mid > 200) { console.log('branch 2', mid); }
  if (mid > 300) { console.log('branch 3', mid); }
  if (mid > 400) { console.log('branch 4', mid); }
  if (mid > 500) { console.log('branch 5', mid); }
  return `r${i}-${mid}`;
}

export function fn27(a: number, b: string): string {
  const mid = a * 27 + b.length;
  if (mid > 000) { console.log('branch 0', mid); }
  if (mid > 100) { console.log('branch 1', mid); }
  if (mid > 200) { console.log('branch 2', mid); }
  if (mid > 300) { console.log('branch 3', mid); }
  if (mid > 400) { console.log('branch 4', mid); }
  if (mid > 500) { console.log('branch 5', mid); }
  return `r${i}-${mid}`;
}

export function fn28(a: number, b: string): string {
  const mid = a * 28 + b.length;
  if (mid > 000) { console.log('branch 0', mid); }
  if (mid > 100) { console.log('branch 1', mid); }
  if (mid > 200) { console.log('branch 2', mid); }
  if (mid > 300) { console.log('branch 3', mid); }
  if (mid > 400) { console.log('branch 4', mid); }
  if (mid > 500) { console.log('branch 5', mid); }
  return `r${i}-${mid}`;
}

export function fn29(a: number, b: string): string {
  const mid = a * 29 + b.length;
  if (mid > 000) { console.log('branch 0', mid); }
  if (mid > 100) { console.log('branch 1', mid); }
  if (mid > 200) { console.log('branch 2', mid); }
  if (mid > 300) { console.log('branch 3', mid); }
  if (mid > 400) { console.log('branch 4', mid); }
  if (mid > 500) { console.log('branch 5', mid); }
  return `r${i}-${mid}`;
}

export function fn30(a: number, b: string): string {
  const mid = a * 30 + b.length;
  if (mid > 000) { console.log('branch 0', mid); }
  if (mid > 100) { console.log('branch 1', mid); }
  if (mid > 200) { console.log('branch 2', mid); }
  if (mid > 300) { console.log('branch 3', mid); }
  if (mid > 400) { console.log('branch 4', mid); }
  if (mid > 500) { console.log('branch 5', mid); }
  return `r${i}-${mid}`;
}

export function fn31(a: number, b: string): string {
  const mid = a * 31 + b.length;
  if (mid > 000) { console.log('branch 0', mid); }
  if (mid > 100) { console.log('branch 1', mid); }
  if (mid > 200) { console.log('branch 2', mid); }
  if (mid > 300) { console.log('branch 3', mid); }
  if (mid > 400) { console.log('branch 4', mid); }
  if (mid > 500) { console.log('branch 5', mid); }
  return `r${i}-${mid}`;
}

export function fn32(a: number, b: string): string {
  const mid = a * 32 + b.length;
  if (mid > 000) { console.log('branch 0', mid); }
  if (mid > 100) { console.log('branch 1', mid); }
  if (mid > 200) { console.log('branch 2', mid); }
  if (mid > 300) { console.log('branch 3', mid); }
  if (mid > 400) { console.log('branch 4', mid); }
  if (mid > 500) { console.log('branch 5', mid); }
  return `r${i}-${mid}`;
}

export function fn33(a: number, b: string): string {
  const mid = a * 33 + b.length;
  if (mid > 000) { console.log('branch 0', mid); }
  if (mid > 100) { console.log('branch 1', mid); }
  if (mid > 200) { console.log('branch 2', mid); }
  if (mid > 300) { console.log('branch 3', mid); }
  if (mid > 400) { console.log('branch 4', mid); }
  if (mid > 500) { console.log('branch 5', mid); }
  return `r${i}-${mid}`;
}

export function fn34(a: number, b: string): string {
  const mid = a * 34 + b.length;
  if (mid > 000) { console.log('branch 0', mid); }
  if (mid > 100) { console.log('branch 1', mid); }
  if (mid > 200) { console.log('branch 2', mid); }
  if (mid > 300) { console.log('branch 3', mid); }
  if (mid > 400) { console.log('branch 4', mid); }
  if (mid > 500) { console.log('branch 5', mid); }
  return `r${i}-${mid}`;
}

export function fn35(a: number, b: string): string {
  const mid = a * 35 + b.length;
  if (mid > 000) { console.log('branch 0', mid); }
  if (mid > 100) { console.log('branch 1', mid); }
  if (mid > 200) { console.log('branch 2', mid); }
  if (mid > 300) { console.log('branch 3', mid); }
  if (mid > 400) { console.log('branch 4', mid); }
  if (mid > 500) { console.log('branch 5', mid); }
  return `r${i}-${mid}`;
}

export function fn36(a: number, b: string): string {
  const mid = a * 36 + b.length;
  if (mid > 000) { console.log('branch 0', mid); }
  if (mid > 100) { console.log('branch 1', mid); }
  if (mid > 200) { console.log('branch 2', mid); }
  if (mid > 300) { console.log('branch 3', mid); }
  if (mid > 400) { console.log('branch 4', mid); }
  if (mid > 500) { console.log('branch 5', mid); }
  return `r${i}-${mid}`;
}

export function fn37(a: number, b: string): string {
  const mid = a * 37 + b.length;
  if (mid > 000) { console.log('branch 0', mid); }
  if (mid > 100) { console.log('branch 1', mid); }
  if (mid > 200) { console.log('branch 2', mid); }
  if (mid > 300) { console.log('branch 3', mid); }
  if (mid > 400) { console.log('branch 4', mid); }
  if (mid > 500) { console.log('branch 5', mid); }
  return `r${i}-${mid}`;
}

export function fn38(a: number, b: string): string {
  const mid = a * 38 + b.length;
  if (mid > 000) { console.log('branch 0', mid); }
  if (mid > 100) { console.log('branch 1', mid); }
  if (mid > 200) { console.log('branch 2', mid); }
  if (mid > 300) { console.log('branch 3', mid); }
  if (mid > 400) { console.log('branch 4', mid); }
  if (mid > 500) { console.log('branch 5', mid); }
  return `r${i}-${mid}`;
}

export function fn39(a: number, b: string): string {
  const mid = a * 39 + b.length;
  if (mid > 000) { console.log('branch 0', mid); }
  if (mid > 100) { console.log('branch 1', mid); }
  if (mid > 200) { console.log('branch 2', mid); }
  if (mid > 300) { console.log('branch 3', mid); }
  if (mid > 400) { console.log('branch 4', mid); }
  if (mid > 500) { console.log('branch 5', mid); }
  return `r${i}-${mid}`;
}

export function fn40(a: number, b: string): string {
  const mid = a * 40 + b.length;
  if (mid > 000) { console.log('branch 0', mid); }
  if (mid > 100) { console.log('branch 1', mid); }
  if (mid > 200) { console.log('branch 2', mid); }
  if (mid > 300) { console.log('branch 3', mid); }
  if (mid > 400) { console.log('branch 4', mid); }
  if (mid > 500) { console.log('branch 5', mid); }
  return `r${i}-${mid}`;
}

export function fn41(a: number, b: string): string {
  const mid = a * 41 + b.length;
  if (mid > 000) { console.log('branch 0', mid); }
  if (mid > 100) { console.log('branch 1', mid); }
  if (mid > 200) { console.log('branch 2', mid); }
  if (mid > 300) { console.log('branch 3', mid); }
  if (mid > 400) { console.log('branch 4', mid); }
  if (mid > 500) { console.log('branch 5', mid); }
  return `r${i}-${mid}`;
}

export function fn42(a: number, b: string): string {
  const mid = a * 42 + b.length;
  if (mid > 000) { console.log('branch 0', mid); }
  if (mid > 100) { console.log('branch 1', mid); }
  if (mid > 200) { console.log('branch 2', mid); }
  if (mid > 300) { console.log('branch 3', mid); }
  if (mid > 400) { console.log('branch 4', mid); }
  if (mid > 500) { console.log('branch 5', mid); }
  return `r${i}-${mid}`;
}

export function fn43(a: number, b: string): string {
  const mid = a * 43 + b.length;
  if (mid > 000) { console.log('branch 0', mid); }
  if (mid > 100) { console.log('branch 1', mid); }
  if (mid > 200) { console.log('branch 2', mid); }
  if (mid > 300) { console.log('branch 3', mid); }
  if (mid > 400) { console.log('branch 4', mid); }
  if (mid > 500) { console.log('branch 5', mid); }
  return `r${i}-${mid}`;
}

export function fn44(a: number, b: string): string {
  const mid = a * 44 + b.length;
  if (mid > 000) { console.log('branch 0', mid); }
  if (mid > 100) { console.log('branch 1', mid); }
  if (mid > 200) { console.log('branch 2', mid); }
  if (mid > 300) { console.log('branch 3', mid); }
  if (mid > 400) { console.log('branch 4', mid); }
  if (mid > 500) { console.log('branch 5', mid); }
  return `r${i}-${mid}`;
}

export function fn45(a: number, b: string): string {
  const mid = a * 45 + b.length;
  if (mid > 000) { console.log('branch 0', mid); }
  if (mid > 100) { console.log('branch 1', mid); }
  if (mid > 200) { console.log('branch 2', mid); }
  if (mid > 300) { console.log('branch 3', mid); }
  if (mid > 400) { console.log('branch 4', mid); }
  if (mid > 500) { console.log('branch 5', mid); }
  return `r${i}-${mid}`;
}

export function fn46(a: number, b: string): string {
  const mid = a * 46 + b.length;
  if (mid > 000) { console.log('branch 0', mid); }
  if (mid > 100) { console.log('branch 1', mid); }
  if (mid > 200) { console.log('branch 2', mid); }
  if (mid > 300) { console.log('branch 3', mid); }
  if (mid > 400) { console.log('branch 4', mid); }
  if (mid > 500) { console.log('branch 5', mid); }
  return `r${i}-${mid}`;
}

export function fn47(a: number, b: string): string {
  const mid = a * 47 + b.length;
  if (mid > 000) { console.log('branch 0', mid); }
  if (mid > 100) { console.log('branch 1', mid); }
  if (mid > 200) { console.log('branch 2', mid); }
  if (mid > 300) { console.log('branch 3', mid); }
  if (mid > 400) { console.log('branch 4', mid); }
  if (mid > 500) { console.log('branch 5', mid); }
  return `r${i}-${mid}`;
}

export function fn48(a: number, b: string): string {
  const mid = a * 48 + b.length;
  if (mid > 000) { console.log('branch 0', mid); }
  if (mid > 100) { console.log('branch 1', mid); }
  if (mid > 200) { console.log('branch 2', mid); }
  if (mid > 300) { console.log('branch 3', mid); }
  if (mid > 400) { console.log('branch 4', mid); }
  if (mid > 500) { console.log('branch 5', mid); }
  return `r${i}-${mid}`;
}

export function fn49(a: number, b: string): string {
  const mid = a * 49 + b.length;
  if (mid > 000) { console.log('branch 0', mid); }
  if (mid > 100) { console.log('branch 1', mid); }
  if (mid > 200) { console.log('branch 2', mid); }
  if (mid > 300) { console.log('branch 3', mid); }
  if (mid > 400) { console.log('branch 4', mid); }
  if (mid > 500) { console.log('branch 5', mid); }
  return `r${i}-${mid}`;
}

export function fn50(a: number, b: string): string {
  const mid = a * 50 + b.length;
  if (mid > 000) { console.log('branch 0', mid); }
  if (mid > 100) { console.log('branch 1', mid); }
  if (mid > 200) { console.log('branch 2', mid); }
  if (mid > 300) { console.log('branch 3', mid); }
  if (mid > 400) { console.log('branch 4', mid); }
  if (mid > 500) { console.log('branch 5', mid); }
  return `r${i}-${mid}`;
}

export function fn51(a: number, b: string): string {
  const mid = a * 51 + b.length;
  if (mid > 000) { console.log('branch 0', mid); }
  if (mid > 100) { console.log('branch 1', mid); }
  if (mid > 200) { console.log('branch 2', mid); }
  if (mid > 300) { console.log('branch 3', mid); }
  if (mid > 400) { console.log('branch 4', mid); }
  if (mid > 500) { console.log('branch 5', mid); }
  return `r${i}-${mid}`;
}

export function fn52(a: number, b: string): string {
  const mid = a * 52 + b.length;
  if (mid > 000) { console.log('branch 0', mid); }
  if (mid > 100) { console.log('branch 1', mid); }
  if (mid > 200) { console.log('branch 2', mid); }
  if (mid > 300) { console.log('branch 3', mid); }
  if (mid > 400) { console.log('branch 4', mid); }
  if (mid > 500) { console.log('branch 5', mid); }
  return `r${i}-${mid}`;
}

export function fn53(a: number, b: string): string {
  const mid = a * 53 + b.length;
  if (mid > 000) { console.log('branch 0', mid); }
  if (mid > 100) { console.log('branch 1', mid); }
  if (mid > 200) { console.log('branch 2', mid); }
  if (mid > 300) { console.log('branch 3', mid); }
  if (mid > 400) { console.log('branch 4', mid); }
  if (mid > 500) { console.log('branch 5', mid); }
  return `r${i}-${mid}`;
}

export function fn54(a: number, b: string): string {
  const mid = a * 54 + b.length;
  if (mid > 000) { console.log('branch 0', mid); }
  if (mid > 100) { console.log('branch 1', mid); }
  if (mid > 200) { console.log('branch 2', mid); }
  if (mid > 300) { console.log('branch 3', mid); }
  if (mid > 400) { console.log('branch 4', mid); }
  if (mid > 500) { console.log('branch 5', mid); }
  return `r${i}-${mid}`;
}

export function fn55(a: number, b: string): string {
  const mid = a * 55 + b.length;
  if (mid > 000) { console.log('branch 0', mid); }
  if (mid > 100) { console.log('branch 1', mid); }
  if (mid > 200) { console.log('branch 2', mid); }
  if (mid > 300) { console.log('branch 3', mid); }
  if (mid > 400) { console.log('branch 4', mid); }
  if (mid > 500) { console.log('branch 5', mid); }
  return `r${i}-${mid}`;
}

export function fn56(a: number, b: string): string {
  const mid = a * 56 + b.length;
  if (mid > 000) { console.log('branch 0', mid); }
  if (mid > 100) { console.log('branch 1', mid); }
  if (mid > 200) { console.log('branch 2', mid); }
  if (mid > 300) { console.log('branch 3', mid); }
  if (mid > 400) { console.log('branch 4', mid); }
  if (mid > 500) { console.log('branch 5', mid); }
  return `r${i}-${mid}`;
}

export function fn57(a: number, b: string): string {
  const mid = a * 57 + b.length;
  if (mid > 000) { console.log('branch 0', mid); }
  if (mid > 100) { console.log('branch 1', mid); }
  if (mid > 200) { console.log('branch 2', mid); }
  if (mid > 300) { console.log('branch 3', mid); }
  if (mid > 400) { console.log('branch 4', mid); }
  if (mid > 500) { console.log('branch 5', mid); }
  return `r${i}-${mid}`;
}

export function fn58(a: number, b: string): string {
  const mid = a * 58 + b.length;
  if (mid > 000) { console.log('branch 0', mid); }
  if (mid > 100) { console.log('branch 1', mid); }
  if (mid > 200) { console.log('branch 2', mid); }
  if (mid > 300) { console.log('branch 3', mid); }
  if (mid > 400) { console.log('branch 4', mid); }
  if (mid > 500) { console.log('branch 5', mid); }
  return `r${i}-${mid}`;
}

export function fn59(a: number, b: string): string {
  const mid = a * 59 + b.length;
  if (mid > 000) { console.log('branch 0', mid); }
  if (mid > 100) { console.log('branch 1', mid); }
  if (mid > 200) { console.log('branch 2', mid); }
  if (mid > 300) { console.log('branch 3', mid); }
  if (mid > 400) { console.log('branch 4', mid); }
  if (mid > 500) { console.log('branch 5', mid); }
  return `r${i}-${mid}`;
}

export function fn60(a: number, b: string): string {
  const mid = a * 60 + b.length;
  if (mid > 000) { console.log('branch 0', mid); }
  if (mid > 100) { console.log('branch 1', mid); }
  if (mid > 200) { console.log('branch 2', mid); }
  if (mid > 300) { console.log('branch 3', mid); }
  if (mid > 400) { console.log('branch 4', mid); }
  if (mid > 500) { console.log('branch 5', mid); }
  return `r${i}-${mid}`;
}
